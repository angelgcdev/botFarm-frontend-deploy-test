# URL del backend en producción o desarrollo (usado por el frontend)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# URL del backend local (por ejemplo, para comunicarse con el servidor local en la PC del usuario)
NEXT_PUBLIC_LOCAL_BACKEND_URL=http://localhost:5000

# Clave secreta para firmar/verificar tokens (si es usada también en el frontend para algo)
JWT_SECRET=tu_clave_secreta_aqui